# Asset

- component used to display assets from /img folder

```vue
<template>
  <base-asset assetName="obrazek.jpg" />
</template>
```

- Displays image located at @/assets/img/
- Transpiles solely into `<img>` tag therefore all `<img>`-related styles are applicable
