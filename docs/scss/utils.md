# Utility scss classes

### .d-mobile-only

```html
<div class="d-mobile-only">This will only be visible on mobile devices</div>
```

### .d-desktop-only

```html
<div class="d-desktop-only">This will only be visible on desktop devices</div>
```
